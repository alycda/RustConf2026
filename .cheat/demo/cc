---
syntax: bash
tags: [ demo ]
---
# 3/4 compile the C caller against the library (one cargo workspace: ../target)
cc tests/c/test_glue.c -L../target/debug -lex2_c_glue -Wl,-rpath,"$PWD/../target/debug" -o test_glue
